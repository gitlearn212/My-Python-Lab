# Get a radius and calculate the are of the circle

from math import pi
radius = float(input('enter a value of radius :'))
# ans = (pi * radius ** 2)
print(f'The area of the circle with radius:{radius} is : {pi * radius **2}')
